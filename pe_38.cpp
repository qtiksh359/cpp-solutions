#include <bits/stdc++.h>
using namespace std;

int main() {
    for(int i = 9999; i >= 1;  i--){
        string s = to_string(i) + to_string(2*i);
        string t = s;
        sort(t.begin(),t.end());
        if(st== "123456789"){
            cout << s << endl;
            i -= 9999;
        }
    }
}